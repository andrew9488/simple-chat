import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    root: {
        display: "flex",
        alignItems: "flex-end",
        padding: 10,
        height: 500,
        width: 350,
        overflowY: "scroll"
    },
});