import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    root: {
        width: 600,
        height: 200,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
    },
    input :{
        width: 200,
        marginRight: 15,
    }
});